package de.intarsys.tools.message.test;

/*
 * we don't want to put the message files in the package that has the tests
 * because they will conflict with the actual application message files. Dummy
 * class to allow regular message loading mechanism
 */
public interface MessageDummy {
	// empty interface
}
